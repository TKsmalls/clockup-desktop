# ClockUp Desktop — Releases

DMG release artifacts for the ClockUp Desktop menu bar app. Source code lives in the private monorepo `TKsmalls/ClockUp` under `ClockUp Desktop/`.

Releases here are produced by `npm run release` and consumed automatically by installed copies via electron-updater.
